/*
#include <vector>
#include <string>
using namespace std;

void bubbleSort(vector<string>& data) {
    int n = data.size();
    for (int i = 0; i < n - 1; ++i) {
        for (int j = 0; j < n - i - 1; ++j) {
            if (data[j].monthly_rent > data[j + 1].monthly_rent) {
                swap(data[j], data[j + 1]);
            }
        }
    }
}
*/